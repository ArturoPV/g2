<h1><%= "#{@user.name}" %></h1>

<% if (@user.house_id != nil) then %>

<h3>Lives in:</h3>

<h4><%= link_to "#{House.find(@user.house_id).name}", house_path(@user.house_id)%></h4>

<% else %>

<h3>Doesn't live anywhere!</h3>

<%end%>
